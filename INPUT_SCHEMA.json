{
    "title": "Scrapy Executor",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "scrape_bundles": {
                    "title": "Scrape Bundles",
                    "type": "boolean",
                    "description": "Scrape [Games, Books and Software] Bundles",
                    "default": true
        },
       "proxyConfig": {
            "title": "Proxy",
            "type": "object",
            "description": "Proxy servers let you bypass website protections, avoid IP address blocking and view content for other countries. Try to use the proxy if you are experiencing timeout errors.",
            "default": {},
            "editor": "proxy"
        }
    },
    "required": ["scrape_bundles"]
}
